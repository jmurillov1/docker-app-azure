apiVersion: v1
kind: Service
metadata:
    name: "jmkub-5808"
    labels:
        app: "jmkub-5808"
spec:
    type: ClusterIP
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "jmkub-5808"